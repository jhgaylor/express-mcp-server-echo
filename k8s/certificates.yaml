apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mcp-echo-tls
  namespace: mcp-echo
spec:
  secretName: mcp-echo-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - echo.inevitable.fyi
